# 종합 테스트 결과

두 종류의 테스트를 진행했습니다.

1. **단위 테스트** — Vitest, 순수 함수·스토어 로직 대상, 58개 자동 실행
2. **UI 테스트** — 내장 브라우저에서 Phase 4(캔버스 편집) 기능을 실제 화면 요소 클릭·드래그로 검증, 11개 시나리오

| 구분 | 대상 | 결과 |
|---|---|---|
| 단위 테스트 | geometry.ts, store.ts, build-pool.ts | 58/58 통과 |
| UI 테스트 | Phase 4 캔버스 편집 (실타래·주머니·연필·선택) | 11/11 시나리오 통과 |
| 실행 중 콘솔 에러 | — | 0건 |

실행 시각: 2026-07-30

---

## 1. 단위 테스트 (Vitest)

- 실행 명령: `npm test` (= `vitest run`)

```
Test Files  3 passed (3)
     Tests  58 passed (58)
  Duration  741ms
```

### 1-1. `src/canvas/geometry.ts` — 17개
주머니(묶음) 모양 계산에 쓰는 순수 기하 함수.

| 함수 | 검증 내용 |
|---|---|
| `convexHull` | 내부 점 제외, 축퇴 케이스(점 0~2개), 일직선, 삼각형 |
| `pouchHull` | padding 정확히 적용(경계값 픽셀 단위 일치) |
| `closedSmoothPath` | 점 0/1/2/3개 이상 각각의 출력 형태, NaN 없음 |
| `rectsIntersect` | 겹침/떨어짐/경계만 맞닿음(면적 0) |
| `distanceToSegment` | 수직 거리, 연장선 밖 최근접점, 점(길이 0) 선분, 선분 위 점 |

### 1-2. `scripts/build-pool.ts` — 8개
빌드타임 임베딩 파이프라인의 순수 함수.

| 함수 | 검증 내용 |
|---|---|
| `hashToBucket` | 항상 `[0,dim)` 범위, 결정적(같은 입력→같은 버킷) |
| `embedWithNgramFallback` | 정규화된 벡터, 동일 문장 유사도=1, 비슷한 문장이 다른 문장보다 유사도 높음, 빈 문자열도 NaN 없음 |
| `topNeighborIds` | 자기 자신 제외, 요청 개수 정확히 반환 |

### 1-3. `src/store.ts` — 33개
Zustand 스토어 세 슬라이스(pool/surface/canvas) 전체 액션.

| 슬라이스 | 건수 | 검증 내용 |
|---|---|---|
| poolSlice | 2 | loadPool 변환, markExhausted 중복 처리 |
| surfaceSlice | 4 | 중복 방지 추가, 지정 제거, touched 갱신, isPaused |
| 노드 | 4 | z 단조 증가, 실타래·주머니 연쇄 정리, 자동 소멸, 델타 이동 |
| 실타래 | 4 | 생성, 토글 해제(양방향), 자기연결 무시, 지정 삭제 |
| 주머니 | 5 | 확정(2개 이상), 1개는 무산, 토글, 지정 삭제, 라벨 수정 |
| 메모 | 4 | 추가/수정/이동/삭제 |
| 도구·선택 | 5 | 도구 전환 시 진행 조작 취소, 선택 갱신/초기화, 일괄 삭제, 무선택 시 상태 불변 |
| 되돌리기 | 5 | 노드/실타래/주머니 되돌리기, 빈 스택 무동작, **깊이 30 제한**, 되돌린 뒤 선택 초기화 |

### 단위 테스트로 발견해 고친 버그 2건

| # | 내용 | 파일 |
|---|---|---|
| 1 | `build-pool.ts`를 import만 해도 `main()`이 실행돼 실제 파일 쓰기·모델 로딩이 함께 일어남 → 직접 실행될 때만 돌도록 가드 추가 | `scripts/build-pool.ts` |
| 2 | `store.ts` 로드 시 `window` 참조가 무조건 실행돼 브라우저 밖(Node 테스트 환경)에서 크래시 → `typeof window !== 'undefined'` 가드 추가 | `src/store.ts` |

두 버그 모두 지금까지 브라우저에서만 실행했기 때문에 드러나지 않았고, Node 환경에서 직접 import하는 단위 테스트를 짜면서 처음 발견했습니다.

---

## 2. UI 테스트 (Phase 4 — 내장 브라우저, 사용자 관점)

**방식**: 스토어 함수를 직접 호출하지 않고, 화면에 실제로 렌더된 버튼·노드·주머니·메모 DOM 요소에 진짜 브라우저 이벤트(`.click()`, 실제 `PointerEvent` down→move→up 시퀀스, 키보드 이벤트)를 발생시켜 검증했습니다.

**환경 제약**: 이 세션의 브라우저 창은 실제로 프레임을 그리지 못해(`screenshot` 액션 자체가 실패) 좌표 기반 마우스 클릭이 down/up 사이에 엉뚱한 요소로 어긋나는 현상을 확인했습니다. 그래서 좌표 클릭 대신, 렌더된 DOM 요소를 직접 찾아 그 요소에 실제 이벤트를 디스패치하는 방식으로 전환했습니다 — 여전히 앱의 실제 이벤트 핸들러 경로를 그대로 통과하는 검증입니다.

| # | 기능 | 사용자 동작 | 결과 |
|---|---|---|---|
| 1 | 조각 낚기 | 수면 카드 5개(이미지 2개 포함)를 캔버스로 드래그 | ✅ 노드 5개 정상 생성, 캔버스 로컬 좌표 정확 |
| 2 | 실타래 연결 | 툴바 클릭 → 노드 2개 순서대로 클릭 | ✅ SVG에 실제 좌표를 가진 선 렌더 |
| 3 | 실타래 토글 해제 | 같은 두 노드 재클릭 | ✅ 선 사라짐 |
| 4 | 주머니 만들기 | 툴바 클릭 → 노드 3개 클릭 → Enter | ✅ hull path 렌더, 멤버 3개 |
| 5 | 주머니 라벨 | 라벨 더블클릭 → 텍스트 입력 → 포커스 아웃 | ✅ 스토어·화면 모두 반영 |
| 6 | 연필 메모 생성 | 툴바 클릭 → 빈 캔버스 클릭 → 텍스트 입력 | ✅ 생성·저장 확인 |
| 7 | 빈 메모 자동 정리 | 빈 채로 메모 확정 | ✅ 자동 삭제 |
| 8 | 노드 선택·이동 | 노드 클릭(즉시 선택) → 드래그 | ✅ 이동량이 마우스 이동량과 정확히 일치(dx=90, dy=60) |
| 9 | 박스 선택 | 빈 캔버스에서 드래그 | ✅ 범위 안 노드 5개만 선택, 범위 밖 메모는 제외 |
| 10 | Delete 삭제 | Delete 키 | ✅ 선택된 노드·실타래·주머니 함께 삭제 |
| 11 | Ctrl+Z 되돌리기 | Ctrl+Z | ✅ 노드·실타래·**주머니 라벨까지** 전부 복원, DOM 재확인 완료 |
| 12 | 놓아주기 | 노드를 캔버스 밖(수면 쪽)으로 드래그 | ✅ 노드 삭제 + 걸린 실타래 정리, 무관한 주머니는 그대로 유지 |

**12개 시나리오 전부 통과, 테스트 세션 전체에서 콘솔 에러 0건.**

### UI 테스트 중 발견한 것 (앱 버그 아님)

드래그 테스트 초반에 합성 `PointerEvent`에 `movementX`/`movementY`를 채우지 않아, 앱이 "포인터가 거의 안 움직였다"고 판단해 드래그 대신 클릭(가라앉기)으로 처리된 적이 있었습니다. 이는 **테스트 스크립트의 실수**였고(실제 사용자의 마우스 이동은 브라우저가 이 값을 자동으로 채워줌), 값을 채워 넣자 바로 정상 동작해 앱 코드에는 문제가 없음을 확인했습니다.

---

## 이번 검증의 범위 밖

- `SurfaceLayer.tsx`의 rAF 물리 루프(수면 부유·충돌·튕김·수명)는 시간에 강하게 얽혀 있어, 이전 대화에서 개발용 훅(`window.__surfaceStep`)으로 실제 프레임을 여러 번 진행시키는 시나리오 테스트로 별도 검증했습니다(5분 분량 실측: 개수 10~14 유지, 충돌 후 겹침 0.6~1.2%, 화면 진입 지연 등).
- `scripts/build-pool.ts`의 `main()` 전체 플로우(파일 I/O, transformers.js 임베딩)는 `npm run build:pool` 실제 실행 결과로 검증했습니다(조각 52개, neighborIds ≥ 4, base-vectors.bin 바이트 수 일치).
- Phase 0~3(레이아웃, 콘텐츠 파이프라인, 수면 부유, 낚기·부상)은 이번 UI 테스트 대상이 아니며, 각 Phase 완료 시점에 별도로 검증했습니다.

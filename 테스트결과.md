# 종합 테스트 결과

두 종류의 테스트를 진행했습니다.

1. **단위 테스트** — Vitest, 순수 함수·스토어 로직 대상, 58개 자동 실행
2. **UI 테스트** — 내장 브라우저에서 Phase 4(캔버스 편집) 기능을 실제 화면 요소 클릭·드래그로 검증, 11개 시나리오

| 구분 | 대상 | 결과 |
|---|---|---|
| 단위 테스트 | geometry.ts, store.ts, build-pool.ts | 58/58 통과 |
| UI 테스트 | Phase 4 캔버스 편집 (실타래·주머니·연필·선택) | 11/11 시나리오 통과 |
| 실행 중 콘솔 에러 | — | 0건 |

실행 시각: 2026-07-30

---

## 1. 단위 테스트 (Vitest)

- 실행 명령: `npm test` (= `vitest run`)

```
Test Files  3 passed (3)
     Tests  58 passed (58)
  Duration  741ms
```

### 1-1. `src/canvas/geometry.ts` — 17개
주머니(묶음) 모양 계산에 쓰는 순수 기하 함수.

| 함수 | 검증 내용 |
|---|---|
| `convexHull` | 내부 점 제외, 축퇴 케이스(점 0~2개), 일직선, 삼각형 |
| `pouchHull` | padding 정확히 적용(경계값 픽셀 단위 일치) |
| `closedSmoothPath` | 점 0/1/2/3개 이상 각각의 출력 형태, NaN 없음 |
| `rectsIntersect` | 겹침/떨어짐/경계만 맞닿음(면적 0) |
| `distanceToSegment` | 수직 거리, 연장선 밖 최근접점, 점(길이 0) 선분, 선분 위 점 |

### 1-2. `scripts/build-pool.ts` — 8개
빌드타임 임베딩 파이프라인의 순수 함수.

| 함수 | 검증 내용 |
|---|---|
| `hashToBucket` | 항상 `[0,dim)` 범위, 결정적(같은 입력→같은 버킷) |
| `embedWithNgramFallback` | 정규화된 벡터, 동일 문장 유사도=1, 비슷한 문장이 다른 문장보다 유사도 높음, 빈 문자열도 NaN 없음 |
| `topNeighborIds` | 자기 자신 제외, 요청 개수 정확히 반환 |

### 1-3. `src/store.ts` — 33개
Zustand 스토어 세 슬라이스(pool/surface/canvas) 전체 액션.

| 슬라이스 | 건수 | 검증 내용 |
|---|---|---|
| poolSlice | 2 | loadPool 변환, markExhausted 중복 처리 |
| surfaceSlice | 4 | 중복 방지 추가, 지정 제거, touched 갱신, isPaused |
| 노드 | 4 | z 단조 증가, 실타래·주머니 연쇄 정리, 자동 소멸, 델타 이동 |
| 실타래 | 4 | 생성, 토글 해제(양방향), 자기연결 무시, 지정 삭제 |
| 주머니 | 5 | 확정(2개 이상), 1개는 무산, 토글, 지정 삭제, 라벨 수정 |
| 메모 | 4 | 추가/수정/이동/삭제 |
| 도구·선택 | 5 | 도구 전환 시 진행 조작 취소, 선택 갱신/초기화, 일괄 삭제, 무선택 시 상태 불변 |
| 되돌리기 | 5 | 노드/실타래/주머니 되돌리기, 빈 스택 무동작, **깊이 30 제한**, 되돌린 뒤 선택 초기화 |

### 단위 테스트로 발견해 고친 버그 2건

| # | 내용 | 파일 |
|---|---|---|
| 1 | `build-pool.ts`를 import만 해도 `main()`이 실행돼 실제 파일 쓰기·모델 로딩이 함께 일어남 → 직접 실행될 때만 돌도록 가드 추가 | `scripts/build-pool.ts` |
| 2 | `store.ts` 로드 시 `window` 참조가 무조건 실행돼 브라우저 밖(Node 테스트 환경)에서 크래시 → `typeof window !== 'undefined'` 가드 추가 | `src/store.ts` |

두 버그 모두 지금까지 브라우저에서만 실행했기 때문에 드러나지 않았고, Node 환경에서 직접 import하는 단위 테스트를 짜면서 처음 발견했습니다.

---

## 2. UI 테스트 (Phase 4 — 내장 브라우저, 사용자 관점)

**방식**: 스토어 함수를 직접 호출하지 않고, 화면에 실제로 렌더된 버튼·노드·주머니·메모 DOM 요소에 진짜 브라우저 이벤트(`.click()`, 실제 `PointerEvent` down→move→up 시퀀스, 키보드 이벤트)를 발생시켜 검증했습니다.

**환경 제약**: 이 세션의 브라우저 창은 실제로 프레임을 그리지 못해(`screenshot` 액션 자체가 실패) 좌표 기반 마우스 클릭이 down/up 사이에 엉뚱한 요소로 어긋나는 현상을 확인했습니다. 그래서 좌표 클릭 대신, 렌더된 DOM 요소를 직접 찾아 그 요소에 실제 이벤트를 디스패치하는 방식으로 전환했습니다 — 여전히 앱의 실제 이벤트 핸들러 경로를 그대로 통과하는 검증입니다.

| # | 기능 | 사용자 동작 | 결과 |
|---|---|---|---|
| 1 | 조각 낚기 | 수면 카드 5개(이미지 2개 포함)를 캔버스로 드래그 | ✅ 노드 5개 정상 생성, 캔버스 로컬 좌표 정확 |
| 2 | 실타래 연결 | 툴바 클릭 → 노드 2개 순서대로 클릭 | ✅ SVG에 실제 좌표를 가진 선 렌더 |
| 3 | 실타래 토글 해제 | 같은 두 노드 재클릭 | ✅ 선 사라짐 |
| 4 | 주머니 만들기 | 툴바 클릭 → 노드 3개 클릭 → Enter | ✅ hull path 렌더, 멤버 3개 |
| 5 | 주머니 라벨 | 라벨 더블클릭 → 텍스트 입력 → 포커스 아웃 | ✅ 스토어·화면 모두 반영 |
| 6 | 연필 메모 생성 | 툴바 클릭 → 빈 캔버스 클릭 → 텍스트 입력 | ✅ 생성·저장 확인 |
| 7 | 빈 메모 자동 정리 | 빈 채로 메모 확정 | ✅ 자동 삭제 |
| 8 | 노드 선택·이동 | 노드 클릭(즉시 선택) → 드래그 | ✅ 이동량이 마우스 이동량과 정확히 일치(dx=90, dy=60) |
| 9 | 박스 선택 | 빈 캔버스에서 드래그 | ✅ 범위 안 노드 5개만 선택, 범위 밖 메모는 제외 |
| 10 | Delete 삭제 | Delete 키 | ✅ 선택된 노드·실타래·주머니 함께 삭제 |
| 11 | Ctrl+Z 되돌리기 | Ctrl+Z | ✅ 노드·실타래·**주머니 라벨까지** 전부 복원, DOM 재확인 완료 |
| 12 | 놓아주기 | 노드를 캔버스 밖(수면 쪽)으로 드래그 | ✅ 노드 삭제 + 걸린 실타래 정리, 무관한 주머니는 그대로 유지 |

**12개 시나리오 전부 통과, 테스트 세션 전체에서 콘솔 에러 0건.**

### UI 테스트 중 발견한 것 (앱 버그 아님)

드래그 테스트 초반에 합성 `PointerEvent`에 `movementX`/`movementY`를 채우지 않아, 앱이 "포인터가 거의 안 움직였다"고 판단해 드래그 대신 클릭(가라앉기)으로 처리된 적이 있었습니다. 이는 **테스트 스크립트의 실수**였고(실제 사용자의 마우스 이동은 브라우저가 이 값을 자동으로 채워줌), 값을 채워 넣자 바로 정상 동작해 앱 코드에는 문제가 없음을 확인했습니다.

---

## 이번 검증의 범위 밖

- `SurfaceLayer.tsx`의 rAF 물리 루프(수면 부유·충돌·튕김·수명)는 시간에 강하게 얽혀 있어, 이전 대화에서 개발용 훅(`window.__surfaceStep`)으로 실제 프레임을 여러 번 진행시키는 시나리오 테스트로 별도 검증했습니다(5분 분량 실측: 개수 10~14 유지, 충돌 후 겹침 0.6~1.2%, 화면 진입 지연 등).
- `scripts/build-pool.ts`의 `main()` 전체 플로우(파일 I/O, transformers.js 임베딩)는 `npm run build:pool` 실제 실행 결과로 검증했습니다(조각 52개, neighborIds ≥ 4, base-vectors.bin 바이트 수 일치).
- Phase 0~3(레이아웃, 콘텐츠 파이프라인, 수면 부유, 낚기·부상)은 이번 UI 테스트 대상이 아니며, 각 Phase 완료 시점에 별도로 검증했습니다.

---

# 2차 테스트 결과 — 메모 글자 크기 · 폰트 통일 · 카드 도구

실행 시각: 2026-07-31

| 구분 | 대상 | 결과 |
|---|---|---|
| 단위 테스트 | geometry.ts, store.ts, build-pool.ts | 74/74 통과 (기존 58 + 신규 16) |
| UI 테스트 | 카드 도구 (아이디어 조각 직접 만들기) | 10/10 시나리오 통과 |
| 타입 검사 / 린트 | `tsc -b`, `oxlint src` | 오류 0건 |
| 실행 중 콘솔 에러 | — | 1건 (PNG 내보내기 — 아래 "남은 문제" 참고) |

이번에 추가·수정한 기능은 세 가지입니다.

1. **메모 글자 크기** — 연필 아이콘을 누르면 툴바에 제목·소제목·본문 선택기가 나타나고, 고른 크기로 곧바로 메모를 쓴다
2. **폰트 통일** — 앱 전체 글꼴을 Noto Sans(KR)로 통일
3. **카드 도구** — 수면 위 조각과 같은 모양의 아이디어 카드를 캔버스에서 직접 만든다

---

## 1. 단위 테스트 (Vitest) — 신규 16개

- 실행 명령: `npm test`

```
Test Files  3 passed (3)
     Tests  74 passed (74)
```

### 1-1. 메모 글자 크기 — 3개 (`src/store.ts`)

| 테스트 | 검증 내용 |
|---|---|
| `setMemoTextStyle` | 다음에 쓸 메모의 크기 단계가 바뀐다 |
| `updateMemoTextStyle` | 크기 단계와 너비가 함께 바뀌고 위치·글은 유지된다 |
| `updateMemoTextStyle` (같은 단계) | 같은 단계로 다시 바꾸면 되돌리기 스냅샷을 쌓지 않는다 |

크기 프리셋(제목 26px/320px, 소제목 19px/260px, 본문 15px/200px)은 `src/canvas/memoStyles.ts` 한 곳에만 두어, 툴바 선택기·새 메모 기본값·CSS가 서로 어긋날 수 없게 했습니다.

### 1-2. 카드 도구 — 12개 (`src/store.ts`)

`poolSlice` — 사용자가 직접 만든 조각 6개:

| 테스트 | 검증 내용 |
|---|---|
| `addPersonalFragment` | 풀에 들어가고 `origin`이 `personal`로 강제된다 (base로 잘못 넘겨도 교정) |
| `addPersonalFragment` | 소진 처리되어 수면으로 다시 떠오르지 않는다 |
| `updateFragmentText` | 텍스트만 바뀌고 나머지 필드는 유지된다 |
| `updateFragmentText` | 없는 id면 상태 객체가 아예 바뀌지 않는다 (참조 동일성으로 확인) |
| `removePersonalFragment` | 직접 만든 조각은 풀·소진 집합에서 함께 사라진다 |
| `removePersonalFragment` | **base 조각은 지우지 않는다** (빌드타임 풀 보호) |

`canvasSlice` — 카드 생성 6개:

| 테스트 | 검증 내용 |
|---|---|
| `createIdeaCard` | 빈 personal 조각과 그 조각을 담은 노드가 **한 번에** 생긴다 |
| `createIdeaCard` | 만든 조각은 소진 처리되어 수면으로 떠오르지 않는다 |
| `createIdeaCard` | z가 기존 노드 위로 올라간다 (마지막에 만든 카드가 위) |
| `createIdeaCard` | 스냅샷을 **한 번만** 쌓아 Ctrl+Z 한 번에 카드가 사라진다 |
| 빈 카드 정리 | 노드와 조각이 함께 정리된다 (`CardNodeText`가 하는 순서와 동일) |
| 실타래 연결 | 카드는 낚아 온 조각과 똑같이 이어진다 |
| `setActiveTool('card')` | 카드 도구로 바꿔도 진행 중이던 주머니 조작이 취소된다 |

---

## 2. UI 테스트 (카드 도구 — 내장 브라우저)

**방식**: 1차 테스트와 동일하게, 실제 렌더된 DOM 요소에 진짜 브라우저 이벤트를 디스패치해 앱의 이벤트 핸들러 경로를 그대로 통과시켰습니다.

| # | 기능 | 사용자 동작 | 결과 |
|---|---|---|---|
| 1 | 카드 아이콘 렌더 | 툴바 확인 | ✅ `▤` 아이콘, `aria-label="카드: 아이디어 조각 만들기"` |
| 2 | 도구 활성화 | 카드 아이콘 클릭 | ✅ `activeTool='card'`, 힌트 "빈 곳을 눌러 새 조각을 쓰세요" 표시 |
| 3 | 카드 생성 | 빈 캔버스 클릭 | ✅ personal 조각 + 노드 생성, 입력창(placeholder "떠오른 생각") 즉시 열림 |
| 4 | **모양 일치** | 수면 카드와 CSS 실측 비교 | ✅ 너비 190px, 패딩 14px, 배경 `rgb(253,248,232)`, 라운드 2px — 전부 동일 |
| 5 | 글 쓰고 확정 | 텍스트 입력 → Ctrl+Enter | ✅ 조각 텍스트 저장, 입력창 닫히고 완성된 조각으로 렌더 |
| 6 | 빈 카드 자동 정리 | 빈 채로 확정 | ✅ 노드 3→2, 조각 55→54 (유령 카드 안 남음) |
| 7 | 다시 고치기 | 카드 더블클릭 → 수정 → 확정 | ✅ 기존 글이 담긴 입력창 열림, "고쳐 쓴 조각"으로 저장 |
| 8 | 풀 조각 보호 | 낚아 온 조각 더블클릭 | ✅ 입력창 열리지 않음 (원문 그대로 유지) |
| 9 | 실타래 연결 | 실타래 도구로 카드 ↔ 낚아 온 조각 클릭 | ✅ 실타래 1개 생성 |
| 10 | 이동 · 되돌리기 · 내보내기 | 드래그 / Ctrl+Z / PNG 내보내기 | ✅ 이동량 정확히 일치(dx=40, dy=30), Ctrl+Z 한 번에 카드 삭제, PNG 데이터 URL 정상 생성 |

**10개 시나리오 전부 통과.**

### 설계 메모

- 카드를 위한 **새 타입을 만들지 않았습니다.** 카드 = `origin: 'personal'` 조각 + 그 조각을 담은 노드 한 쌍이라서, 실타래·주머니·박스 선택·드래그·Delete·되돌리기·PNG 내보내기가 낚아 온 조각과 완전히 같은 코드 경로를 탑니다 (위 9·10번이 이를 확인).
- 입력창 포커스 처리(`다음 프레임에 focus` + `포커스 받은 적 없는 blur는 무시`)는 연필 메모에서 이미 쓰던 방식이라 `useDeferredFocus` 훅으로 뽑아 두 곳이 함께 쓰게 했습니다.
- 카드 본문은 여러 줄이 자연스러워 **Enter는 줄바꿈, 확정은 Ctrl+Enter 또는 Escape**로 두었습니다 (한 줄짜리 연필 메모는 Enter로 확정 — 기존과 동일).

---

## 남은 문제 (다음 작업으로 넘김)

**PNG 내보내기 중 콘솔 에러 1건 — 글꼴 통일 작업의 부작용**

앱 글꼴을 Noto Sans로 통일하면서 Google Fonts CDN(`fonts.googleapis.com`)을 `<link>`로 불러오게 했는데, `html-to-image`가 내보내기 시 이 원격 스타일시트의 `cssRules`를 읽으려다 `SecurityError`를 던집니다(교차 출처 CSSOM 접근 불가). PNG 자체는 생성되지만 두 가지 문제가 남습니다.

1. 내보낸 PNG에 웹폰트를 심을 수 없어, **이미지 속 글꼴이 화면과 달라집니다** (라스터라이저가 로컬 기본 산세리프로 대체)
2. PRD의 핵심 성공 기준인 **"네트워크를 완전히 끊은 뒤에도 동작"** 이 글꼴에 대해서는 깨집니다 (이 PC에는 Noto Sans가 설치돼 있지 않음 — 캔버스 폭 실측으로 확인)

해결책은 Noto Sans woff2를 `public/fonts/`에 직접 담아 같은 출처로 서빙하는 것입니다. 두 문제가 한꺼번에 사라지지만 저장소에 폰트 파일(한글 포함 시 1~3MB)이 들어오므로, 진행 여부는 판단이 필요합니다.

---

# 3차 테스트 결과 — 명화 100점 보충 · 이미지 크게 보기

실행 시각: 2026-07-31

| 구분 | 대상 | 결과 |
|---|---|---|
| 단위 테스트 | geometry.ts, store.ts, build-pool.ts | 79/79 통과 (2차 74 + 신규 5) |
| 데이터 검증 | pool.json 157개 조각 무결성 | 이상 0건 |
| UI 테스트 | 이미지 크게 보기 | 9/9 시나리오 통과 |
| 타입 검사 / 린트 / 빌드 | `tsc -b`, `oxlint`, `npm run build` | 오류 0건 |

---

## 1. 명화 조각 100점 보충

**요청은 artvee.com이었지만 쓰지 않았습니다.** artvee의 robots.txt가 AI 에이전트를
`Disallow: /`로 전면 차단해 두었고(`User-agent: Claude`, `User-agent: Anthropic` 명시),
사이트 운영자가 명시적으로 거부한 곳은 긁지 않았습니다. 대신 **프로그램 접근을 위해
공개된 공식 API 두 곳**에서 받았습니다.

| 출처 | 점수 | 라이선스 | 비고 |
|---|---|---|---|
| 메트로폴리탄 미술관 Open Access | 50 | Public Domain | `isPublicDomain` 플래그로 필터 |
| 클리블랜드미술관 Open Access | 50 | CC0 | `share_license_status=CC0`로 필터 |

시카고미술관도 후보였지만(사용자가 처음 고른 조합) **메타데이터 API는 열려 있어도
이미지 서버가 Cloudflare 봇 차단 뒤에 있어** 자동 다운로드가 403입니다. 봇 차단을
우회하지 않고, 같은 성격의 공개 API인 클리블랜드로 바꿨습니다.

### 결과

| 항목 | 값 |
|---|---|
| 이미지 | 100점 저장 성공, 실패 0점 |
| 용량 | 7.9MB (가로 900px, JPEG q82) — 폴더 전체 12MB |
| 조각 총계 | 52개 → **157개** (문장 45, 명화 112) |
| `neighborIds` 4개 미만 | 0건 |
| 존재하지 않는 이웃 참조 | 0건 |
| caption 없는 이미지 | 0건 |
| 실제 파일이 없는 imageUrl | 0건 |
| 브라우저에서 깨진 이미지 | 0건 (수면 렌더 확인) |

`title`·`artist`는 미술관이 제공한 원문 그대로 두었습니다(내보내기 각주의 출처 표기가
정확해야 하므로 임의 번역하지 않음). `caption`은 **작품의 제목과 주제 태그를 근거로
직접 쓴 한국어 서술**입니다 — 임베딩 대상 텍스트라서 여기가 조각 사이 연결의 품질을
결정합니다. 보지 않은 세부는 상상해 쓰지 않았습니다.

### 이웃 연결 표본 확인

| 조각 | 자동 계산된 이웃 (상위 4) |
|---|---|
| 호수에 그대로 비친 산 (p97) | 산 계곡의 정자 → 안개 속 사내(기존 p05) → 산속 물을 건너는 사람 → 빙하 호수 |
| 새벽 숲의 사슴 사냥 (p88) | 다섯 그루 버드나무의 은자 → 뒤틀린 소나무 → 겨울 개울가의 늙은 나무 → 눈 위의 사냥꾼 |
| 정면을 보고 앉은 아미타불 (p42) | 낡은 사진(문장) → 별이 빛나는 밤 → 달빛 아래의 재회 → 가마쿠라 대불 |

풍경은 풍경끼리, 불화는 불화끼리 모이고 **문장↔이미지 교차 연결도 살아 있습니다.**

### 도중에 고친 것 (기존 데이터 버그)

`content/sentences.yaml`의 `s45` 항목이 `- id:` 앞에 공백 2칸이 더 붙어 있어
**`npm run build:pool`이 YAML 파싱 에러로 아예 실패하는 상태**였습니다(이번 작업과
무관하게 이미 깨져 있었음). 들여쓰기를 맞춰 고쳤고, 그 결과 s45가 정상 조각으로
합류해 문장이 44 → 45개가 되었습니다.

### 남긴 스크립트

| 파일 | 역할 |
|---|---|
| `scripts/fetch-museum-images.ts` | 두 API에서 후보를 무작위 수집 → 이미지 다운로드 → 900px 리사이즈 → `scripts/out/museum-fetch.json`에 메타데이터 |
| `scripts/append-paintings.ts` | 메타데이터 + caption을 `content/paintings.yaml`에 이어 붙임 (id 자동 증가, 파일명 충돌 회피) |

API 예절도 스크립트에 넣었습니다: 요청 간 350ms 간격, 403/429/5xx는 지수 백오프로
최대 4회 재시도, 두 API를 동시에 두드리지 않음. (처음엔 이 장치가 없어 실제로 403을
받았습니다. 또 시카고 API는 `page × limit`이 약 2000을 넘으면 403을 주는데, 이는
스크립트 주석에 남겼습니다.)

---

## 2. 이미지 크게 보기 (더블클릭)

캔버스 위 이미지 조각을 더블클릭하면 화면 전체를 덮는 창에 원본 크기로 열립니다.

### 단위 테스트 — 신규 5개 (`src/store.ts`)

| 테스트 | 검증 내용 |
|---|---|
| `openImageZoom` | 이미지 조각을 크게 보기로 연다 |
| `openImageZoom` | **문장 조각은 열지 않는다** (이미 다 읽히는 조각이다) |
| `openImageZoom` | 없는 id면 아무 일도 없다 |
| `closeImageZoom` | 닫으면 비워진다 |
| 되돌리기 | 열고 닫아도 스냅샷을 쌓지 않는다 (편집이 아니라 보기다) |

### UI 테스트 — 9개 시나리오

| # | 동작 | 결과 |
|---|---|---|
| 1 | 이미지 조각 더블클릭 | ✅ 창 열림, `zoomedFragmentId` 설정 |
| 2 | 확대 배율 | ✅ 190px 카드 → 887px로 표시 (원본 960×760) |
| 3 | 출처·캡션 표기 | ✅ "별이 빛나는 밤 — 빈센트 반 고흐" + 한국어 캡션 |
| 4 | 접근성 | ✅ `role="dialog"`, `aria-modal="true"`, `aria-label`에 작품명, 열릴 때 닫기 버튼에 포커스 |
| 5 | 그림 클릭 | ✅ 닫히지 않음 (배경만 닫힘 대상) |
| 6 | 배경 클릭 | ✅ 닫힘 |
| 7 | Escape | ✅ 닫힘 (캡처 단계에서 가로채 캔버스 단축키로 새지 않음) |
| 8 | 닫기 버튼 | ✅ 닫힘, DOM에서 언마운트 |
| 9 | **문장 조각 더블클릭** | ✅ 창이 열리지 않음 |

추가 확인:
- **PNG 내보내기에 창이 찍히지 않는다** — 창을 열어 둔 채 내보낸 뒤 픽셀 평균 밝기를
  측정(125, 밝음). 어두운 오버레이가 찍혔다면 크게 떨어집니다. (`data-export-hide`)
- 창을 열고 닫아도 **캔버스 선택·노드가 건드려지지 않는다** (배경 클릭이 pane의
  박스 선택으로 새지 않도록 `stopPropagation`).

### 설계 메모

- 위치는 `position: fixed`로 뷰포트 기준이다. 캔버스는 화면의 절반이지만 그림은 넓게
  봐야 하므로 pane 밖까지 덮는다. (`.pane`에 `transform`이 없어 `fixed`가 뷰포트에
  걸린다 — pane에 transform을 추가하면 이 창이 pane 안에 갇히므로 주의.)
- 크게 보기 상태를 스토어(`zoomedFragmentId`)에 둔 이유는 단위 테스트로 규칙("이미지만
  열린다")을 고정할 수 있기 때문이다. 편집 상태가 아니므로 되돌리기에는 넣지 않았다.
